--extra-index-url https://download.pytorch.org/whl/cpu
--find-links https://download.pytorch.org/whl/cpu/torch_stable.html

# Core dependencies with specific versions that don't require compilation
torch==2.5.1+cpu
transformers==4.36.0
tokenizers==0.15.0 --only-binary :all:
numpy==1.24.3
requests==2.31.0
beautifulsoup4==4.12.2
flask==2.3.3
gunicorn==20.1.0

# Pre-built wheels for problematic packages
regex==2023.10.3 --only-binary :all:
tqdm==4.66.1 --only-binary :all:
packaging==23.2 --only-binary :all:
filelock==3.13.1 --only-binary :all:
typing-extensions==4.8.0 --only-binary :all:
sympy==1.12 --only-binary :all:
networkx==3.2.1 --only-binary :all: